{
  "name": "pcf",
  "version": "0.1.0",
  "private": true,
  "description": "PCF — Pierre-Ciseaux-Feuille, le sport numérique",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "script-voix": "node outils/generer-script.js",
    "demarrer": "npx --yes serve app"
  },
  "engines": { "node": ">=20" }
}
